---
phase: 02-numerical-implementation-and-benchmark-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [analysis/gw_spectrum.py, tests/test_gw_kernels.py]
interactive: false

conventions:
  units: "natural"
  metric: "(-+++)"
  fourier: "physics"
  coordinates: "Comoving x, physical time t"

dimensional_check:
  Omega_GW: '[dimensionless]'
  f: '[energy] (in source frame), [Hz] (in observer frame)'

approximations:
  - name: "Envelope Approximation (Bubbles)"
    parameter: "kappa_bub"
    validity: "Vacuum transitions or negligible plasma interaction"
  - name: "Sound Shell Model (SSM)"
    parameter: "kappa_sw"
    validity: "Acoustic-dominated thermal transitions"
  - name: "Kolmogorov-type Turbulence"
    parameter: "kappa_turb"
    validity: "Late-time stationary turbulence"

contract:
  scope:
    question: "Can the unified GW spectrum formula be implemented numerically with consistent IR/UV power laws and correct redshifting?"
  claims:
    - id: claim-kernel-consistency
      statement: "The implemented source kernels reproduce the required k^3 IR scaling and mechanism-specific UV slopes (p=1 for bubbles, p=3/4 for sound, p=11/3 for turbulence)."
      deliverables: [deliv-gw-spectrum-lib, deliv-gw-tests]
      acceptance_tests: [test-uv-ir-slopes]
    - id: claim-redshift-accuracy
      statement: "The transformation from source-frame (k, Omega_GW*) to observer-frame (f, Omega_GW h^2) correctly accounts for cosmic expansion and g_* degrees of freedom."
      deliverables: [deliv-gw-spectrum-lib]
      acceptance_tests: [test-redshift-check]
  deliverables:
    - id: deliv-gw-spectrum-lib
      kind: code
      path: "analysis/gw_spectrum.py"
      description: "Python library for computing unified GW spectra and redshifting."
      must_contain: ["OmegaBub", "OmegaSW", "OmegaTurb", "redshift_to_observer"]
    - id: deliv-gw-tests
      kind: code
      path: "tests/test_gw_kernels.py"
      description: "Unit tests verifying kernels, limits, and slopes."
      must_contain: ["test_ir_slope", "test_uv_slope", "test_limits"]
  acceptance_tests:
    - id: test-uv-ir-slopes
      subject: claim-kernel-consistency
      kind: benchmark
      procedure: "Compute Omega(k) at k << k_p and k >> k_p and extract local slopes using numerical differentiation."
      pass_condition: "IR slope within 5% of +3, UV slopes within 5% of specified mechanism values."
    - id: test-single-source-limit
      subject: claim-kernel-consistency
      kind: benchmark
      procedure: "Set two of the three kappa efficiency factors to zero and verify Omega_tot matches the remaining kernel."
      pass_condition: "Maximum relative difference < 1e-10."
    - id: test-redshift-check
      subject: claim-redshift-accuracy
      kind: benchmark
      procedure: "Compute (f, Omega_GW h^2) for a known benchmark (e.g., T_*=100 GeV) and verify against analytic redshifting formulas in 02-RESEARCH.md."
      pass_condition: "Peak frequency and amplitude match within 1% of the expected redshifted values."

---

<objective>
Implement the unified GW spectral density formula numerically in Python and verify its consistency with physical limits (IR/UV scaling) and the observer-frame redshifting requirements.

Purpose: Build the core numerical engine needed for the project's benchmark validation and future sensitivity studies.
Output: A validated spectral library (`analysis/gw_spectrum.py`) and a suite of unit tests (`tests/test_gw_kernels.py`).
</objective>

<execution_context>
@/Users/jonatan/.gemini/get-physics-done/workflows/execute-plan.md
@/Users/jonatan/.gemini/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/01-unified-formalism-for-gw-sources/01-01-SUMMARY.md
@.gpd/phases/01-unified-formalism-for-gw-sources/01-02-SUMMARY.md
@.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-RESEARCH.md
@.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-EXPERIMENT-DESIGN.md
@derivations/unified_gw_formalism.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Spectral Kernels and Unified Spectrum</name>
  <files>analysis/gw_spectrum.py</files>
  <action>
    Implement the source kernels for GW spectral density in a Python class or functional library.

    1. **Bubble kernel (Envelope Approximation):** Eq (01.3) from `derivations/unified_gw_formalism.tex`.
       $\Omega_{\text{bub}}(k) \approx \mathcal{A}_{\text{bub}} \frac{3.8 (k/k_p)^{2.8}}{1 + 2.8 (k/k_p)^{3.8}}$ with $k_p = k_{\text{bub}}$.
    2. **Sound Wave kernel (Sound Shell Model):** Eq (01.4) and the Hindmarsh (2019) fitting function:
       $\tilde{\Omega}_{\text{sw}}(k) \approx (k/k_p)^3 \left( \frac{7}{4 + 3 (k/k_p)^2} \right)^{7/2}$.
       Include the suppression factor $\Upsilon = \min(1, H_* \tau_{\text{sw}})$.
    3. **Turbulence kernel (Kolmogorov):** Eq (01.5):
       $\Omega_{\text{turb}}(k) \approx \mathcal{A}_{\text{turb}} \frac{(k/k_{\text{turb}})^3}{[1 + (k/k_{\text{turb}})]^{11/3} (1 + 8\pi k / H_*)}$.
    4. **Unified Formula:** $\Omega_{\text{GW}}(k) = \Omega_{\text{bub}}(k) + \Omega_{\text{sw}}(k) + \Omega_{\text{turb}}(k)$.

    Ensure the parameters $\alpha$, $v_w$, $\beta/H_*$, and the efficiency factors $\kappa_i$ are passed correctly.
    Use vectorized NumPy operations for $k$-grid evaluations.
  </action>
  <verify>
    1. Check $k^3$ scaling numerically for $k \ll k_p$ for all kernels.
    2. Check that $\Omega_{GW} > 0$ for all $k$.
    3. Dimensional analysis: ensure the output is dimensionless.
  </verify>
  <done>Core spectral kernels and unified formula implemented in `analysis/gw_spectrum.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redshifting to Observer Frame</name>
  <files>analysis/gw_spectrum.py</files>
  <action>
    Add a function `redshift_to_observer(k_source, omega_source, T_star, g_star)` that transforms the source-frame spectrum to the present-day observer frame $(f, \Omega_{GW} h^2)$.

    Use the formulas from `.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-RESEARCH.md`:
    1. **Frequency:** $f = 1.65 \times 10^{-5} \text{Hz} \cdot \left( \frac{f_*}{H_*} \right) \left( \frac{T_*}{100 \text{ GeV}} \right) \left( \frac{g_*}{100} \right)^{1/6}$, where $f_*/H_* = k_*/(2\pi H_*)$.
    2. **Energy Density:** $\Omega_{GW} h^2 = 1.67 \times 10^{-5} \cdot \Omega_{GW}^* \left( \frac{g_*}{100} \right)^{-1/3}$.

    Assume the standard radiation-dominated expansion history since the transition.
  </action>
  <verify>
    1. For $T_*=100$ GeV, $g_*=100$, and $f_*/H_*=10$, verify $f \approx 1.65 \times 10^{-4}$ Hz.
    2. For $\Omega_{GW}^* = 10^{-5}$, verify $\Omega_{GW} h^2 \approx 1.67 \times 10^{-10}$.
  </verify>
  <done>Redshifting logic implemented and checked for consistency.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Testing and Physical Limit Verification</name>
  <files>tests/test_gw_kernels.py</files>
  <action>
    Create a test suite using `pytest` to verify the physical properties of the implementation.

    1. **test_ir_slope:** Compute $d\ln \Omega / d\ln k$ at $k/k_p = 0.01$ and assert it is close to 3.
    2. **test_uv_slope:** Compute $d\ln \Omega / d\ln k$ at $k/k_p = 100$ and assert it matches mechanism-specific decay rates ($p \sim 1$ for bubbles, $p \sim 4$ for sound, $p \sim 5/3$ for turbulence energy density).
    3. **test_limits:** Set $\kappa_{\text{sw}} = 0, \kappa_{\text{turb}} = 0$ and verify $\Omega_{\text{tot}} \approx \Omega_{\text{bub}}$. Repeat for other kernels.
    4. **test_normalization:** Ensure the peak amplitude matches the analytic expectation $\mathcal{A}_i$ (within the kernel's normalization factor).
  </action>
  <verify>
    Run `pytest tests/test_gw_kernels.py` and ensure all tests pass.
  </verify>
  <done>Unit tests pass, confirming physical consistency of the spectral implementation.</done>
</task>

</tasks>

<verification>
- IR scaling $\Omega \sim k^3$ verified for all kernels.
- UV scaling consistent with mechanism-specific expectations.
- Single-source limits correctly recovered.
- Redshifting matches analytic benchmarks from research.
</verification>

<success_criteria>
Spectral density library `analysis/gw_spectrum.py` implemented and verified by `tests/test_gw_kernels.py`. All physical slopes and single-source limits are numerically correct.
</success_criteria>

<output>
After completion, create `.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-01-SUMMARY.md`
</output>
