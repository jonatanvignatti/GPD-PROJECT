---
phase: 02-numerical-implementation-and-benchmark-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [analysis/error_budget.md, data/benchmarks/hindmarsh_comparison.pdf, data/benchmarks/redshifted_spectra.pdf]
interactive: false

conventions:
  units: "natural"
  metric: "(-+++)"
  fourier: "physics"

dimensional_check:
  Omega_peak: '[dimensionless]'
  k_peak: '[energy] (source frame)'

approximations:
  - name: "Logarithmic k-grid"
    parameter: "N_k"
    validity: "Capturing peak and asymptotic slopes (k in [1e-1, 1e4] H_*)"

contract:
  scope:
    question: "Does the numerical implementation of the unified GW spectrum agree with the Hindmarsh (2015) benchmark within 5%?"
  claims:
    - id: claim-numerical-convergence
      statement: "The integrated spectrum and peak values are stable within 1% when varying k-grid resolution from N_k=50 to N_k=200."
      deliverables: [deliv-error-budget-update]
      acceptance_tests: [test-convergence-stability]
    - id: claim-benchmark-validation
      statement: "The computed GW spectra for the benchmark cases (alpha=0.01, vw=0.92, etc.) match Hindmarsh (2015) Fig 7 and Eq 34 results within 5% accuracy."
      deliverables: [deliv-comparison-plot, deliv-redshifted-plot]
      acceptance_tests: [test-peak-accuracy, test-amplitude-accuracy]
  deliverables:
    - id: deliv-error-budget-update
      kind: analysis
      path: "analysis/error_budget.md"
      description: "Updated error budget documenting numerical convergence and systematic uncertainties."
      must_contain: ["convergence", "systematic error"]
    - id: deliv-comparison-plot
      kind: plot
      path: "data/benchmarks/hindmarsh_comparison.pdf"
      description: "Plot comparing the code output vs Hindmarsh (2015) digitized data/fitting formula."
    - id: deliv-redshifted-plot
      kind: plot
      path: "data/benchmarks/redshifted_spectra.pdf"
      description: "Redshifted spectra in the observer frame (f, Omega_GW h^2) for various LISA/PTA scenarios."
  acceptance_tests:
    - id: test-convergence-stability
      subject: claim-numerical-convergence
      kind: numerical
      procedure: "Run the benchmark case with N_k=50, 100, and 200. Compute the relative change in the integrated energy density."
      pass_condition: "Relative change between N_k=100 and N_k=200 is < 1%."
    - id: test-peak-accuracy
      subject: claim-benchmark-validation
      kind: numerical
      procedure: "Compare k_peak from the code with k_peak approx 2/R_* for benchmark wall velocities (vw=0.5, 0.92)."
      pass_condition: "Relative difference < 5%."
    - id: test-amplitude-accuracy
      subject: claim-benchmark-validation
      kind: numerical
      procedure: "Compare the peak amplitude with the fitting formula Eq (34) in Hindmarsh (2015)."
      pass_condition: "Relative difference < 5%."

---

<objective>
Validate the numerical implementation against the Hindmarsh (2015) simulation benchmarks and quantify the numerical error budget. Produce production-quality redshifted spectra for physical phase transition scenarios.

Purpose: Ensure the code is research-grade and provides accurate predictions for future modeling sensitivity studies (Phase 3).
Output: Updated error budget analysis and validation plots against established literature.
</objective>

<execution_context>
@/Users/jonatan/.gemini/get-physics-done/workflows/execute-plan.md
@/Users/jonatan/.gemini/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-01-SUMMARY.md
@.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-RESEARCH.md
@.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-EXPERIMENT-DESIGN.md
@analysis/gw_spectrum.py
@analysis/error_budget.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conduct Numerical Convergence Study</name>
  <files>analysis/error_budget.md</files>
  <action>
    Perform a convergence test of the numerical spectrum by varying the grid resolution $N_k$ and integration tolerance.

    1. Use the benchmark case $(v_w=0.92, \alpha=0.01, H_* R_* = 0.1)$.
    2. Evaluate the spectrum for $N_k \in \{50, 100, 200, 400\}$ points per decade.
    3. Compute the integrated energy density $I = \int \Omega_{GW} d\ln k$ for each case.
    4. Compute the relative error $\Delta I / I$ relative to the $N_k=400$ case.
    5. Document the results in `analysis/error_budget.md`, defining the recommended resolution for production runs (targeting 1% stability).

    Repeat for integration tolerance `eps` in $\{10^{-3}, 10^{-6}\}$.
  </action>
  <verify>
    1. Relative error between $N_k=100$ and $N_k=200$ should be $< 1\%$.
    2. Check that the UV tail is sufficiently resolved (cutoff at $100 \times k_p$).
  </verify>
  <done>Convergence study completed and documented in `analysis/error_budget.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Validate Against Hindmarsh (2015) Benchmarks</name>
  <files>data/benchmarks/hindmarsh_comparison.pdf</files>
  <action>
    Compare the implemented code against the primary benchmark: Hindmarsh et al. (2015).

    1. Execute the 6 cases from `02-EXPERIMENT-DESIGN.md`:
       - Grid: $v_w \in \{0.5, 0.92\}$, $\alpha \in \{0.01, 0.05, 0.1\}$.
    2. Compare the resulting $\Omega_{sw}(k)$ with the fitting formula Eq (34) and digitized points from Fig. 7 of Hindmarsh (2015).
    3. Verify that the peak frequency $k_p$ scales correctly with $1/R_*$.
    4. Generate a comparison plot showing the code output overlaid with the benchmark data.
  </action>
  <verify>
    1. Peak amplitude matches within 5% of Eq (34).
    2. Peak frequency matches within 5% of simulation results.
    3. UV/IR slopes match the benchmark qualitative features.
  </verify>
  <done>Code validated against Hindmarsh (2015) with documented 5% agreement.</done>
</task>

<task type="auto">
  <name>Task 3: Production of Redshifted Observer-Frame Spectra</name>
  <files>data/benchmarks/redshifted_spectra.pdf</files>
  <action>
    Generate redshifted spectra plots for the observer frame $(f, \Omega_{GW} h^2)$.

    1. For the benchmark cases, redshift the spectra to the observer frame assuming $T_* = 100$ GeV and $g_* = 100$.
    2. Plot the results in the frequency range $10^{-6}$ Hz to $1$ Hz.
    3. Include sensitivity curves for LISA and future PTA if available (optional/schematic).
    4. Save the resulting figure to `data/benchmarks/redshifted_spectra.pdf`.

    This demonstrates the code's ability to produce physical observables.
  </action>
  <verify>
    1. Peak frequency for $T_*=100$ GeV should be in the millihertz range (LISA band).
    2. Amplitude scales correctly with the redshift factor $\sim 1.67 \times 10^{-5}$.
  </verify>
  <done>Production redshifted spectra generated for benchmark scenarios.</done>
</task>

</tasks>

<verification>
- Numerical stability within 1% confirmed by convergence study.
- Direct agreement within 5% of Hindmarsh (2015) benchmarks for peak amplitude and frequency.
- Integrated energy density converges and obeys physical bounds.
</verification>

<success_criteria>
Validation against Hindmarsh (2015) confirms the code is correct within 5%. Convergence study establishes a robust numerical error budget. Observer-frame plots are produced for standard EWPT scenarios.
</success_criteria>

<output>
After completion, create `.gpd/phases/02-numerical-implementation-and-benchmark-validation/02-02-SUMMARY.md`
</output>
